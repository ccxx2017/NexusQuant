# FastAPI and Uvicorn
fastapi>=0.90.0
uvicorn[standard]>=0.20.0

# Pydantic (FastAPI uses it extensively for data validation and settings)
pydantic>=1.10.0,<2.0.0
# 添加
python-dotenv>=1.0.0

# Tushare (for financial data)
tushare>=1.2.80

# Backtrader (for backtesting)
backtrader>=1.9.76.123
matplotlib>=3.0.0

# Pandas (for data manipulation, Tushare and Backtrader rely on it)
pandas>=1.3.0

ta-lib==0.6.3
# Optional: Database (if you plan to use a relational database)
# psycopg2-binary # For PostgreSQL
# databases[postgresql] # Async DB access with SQLAlchemy core
# SQLAlchemy>=1.4.0,<2.0.0 # ORM, if you choose to use it with Pydantic v1 compatible FastAPI
# python-jose[cryptography] # For JWT token handling (if you add authentication)
# passlib[bcrypt] # For password hashing (if you add authentication)

# Optional: For asynchronous tasks (if backtesting is long)
# celery>=5.0.0
# redis>=4.0.0 # Or RabbitMQ client

# Optional: For better development experience
# python-dotenv>=0.20.0 # To load .env files (though pydantic-settings handles this)
# black>=22.0.0 # Code formatter
# flake8>=5.0.0 # Linter
# isort>=5.0.0 # Import sorter